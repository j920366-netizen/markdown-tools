[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "markdown-tools"
version = "0.1.0"
description = "Small Python utilities for processing Markdown files"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
